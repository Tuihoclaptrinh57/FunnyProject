#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx commitlint --edit $1 2>/dev/null || echo "commitlint skip (install deps first)"
# mvn spotless:check -pl backend -q 2>/dev/null || echo "spotless skip"
